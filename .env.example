# MongoDB Atlas connection string (required for production/Vercel)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>

# Local development port (optional, defaults to 3000)
PORT=3000
